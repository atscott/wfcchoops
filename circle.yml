dependencies:
  post:
    - npm run build:prod
    - zip -r dist.zip dist
test:
  post:
    - bash <(curl -s https://codecov.io/bash)
general:
  artifacts:
    - "dist"
    - "dist.zip"
deployment:
  production:
    branch: master
    commands:
      - ./deploy_prod.sh
